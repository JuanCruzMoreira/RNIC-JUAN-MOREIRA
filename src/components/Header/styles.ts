import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 12,
  },
  title: {
    color: '#000000',
    alignSelf: 'center',
    fontSize: 24,
    fontWeight: '600',
  },
});

export default styles;
